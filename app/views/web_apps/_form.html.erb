<%= form_for(web_app) do |f| %>
  <% if web_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_app.errors.count, "error") %> prohibited this web_app from being saved:</h2>

      <ul>
      <% web_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field form-group">
    <%= f.label(:page, t("controller.webapp.page")) %><br />
    <%= f.text_field :page, autofocus: true, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label(:description, t("controller.webapp.description") ) %><br />
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit t("buttons.save"), class: "btn btn-success" %>
  </div>
<% end %>
